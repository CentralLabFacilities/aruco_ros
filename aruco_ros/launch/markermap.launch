<launch>

    <arg name="markerYAML"        default="/tmp/marker_map_config.yaml"/>
    <arg name="markerSize"      default="0.05"/>    <!-- in m -->
    <arg name="eye"             default="eye_left"/>
    <arg name="ns" default="/floka/$(arg eye)" />
    <arg name="marker_frame"    default="aruco_marker_frame"/>
    <arg name="ref_frame"       default=""/>  <!-- leave empty and the pose will be published wrt param parent_name -->
    <arg name="corner_refinement" default="LINES" /> <!-- NONE, HARRIS, LINES, SUBPIX -->

    <group ns="$(arg ns)">
      <node pkg="aruco_ros" type="markermap" name="aruco_markermap">
        <remap from="/camera_info" to="/floka/$(arg eye)/camera_info" />
        <remap from="/image" to="/floka/$(arg eye)/image_mono" />
        <param name="image_is_rectified" value="True"/>
        <param name="marker_size"        value="$(arg markerSize)"/>
        <param name="marker_set_yaml"          value="$(arg markerYAML)"/>
        <param name="reference_frame"    value="$(arg ref_frame)"/>   <!-- frame in which the marker pose will be refered -->
        <param name="camera_frame"       value="floka_$(arg eye)_optical_frame"/>
        <param name="marker_frame"       value="$(arg marker_frame)" />
        <param name="corner_refinement"  value="$(arg corner_refinement)" />
      </node>
    </group>
</launch>
